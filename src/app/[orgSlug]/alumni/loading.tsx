import { MembersPageSkeleton } from "@/components/skeletons/pages";

/** Route-level loading UI for the alumni page (Next.js loading.tsx convention). */
export default function Loading() {
  return <MembersPageSkeleton />;
}
