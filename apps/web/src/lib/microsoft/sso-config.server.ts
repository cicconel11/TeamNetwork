/**
 * Whether Microsoft login (Supabase Auth Azure provider) is enabled.
 * Always true — the provider is configured in the Supabase Dashboard.
 */
export function isMicrosoftLoginEnabled(): boolean {
  return true;
}
