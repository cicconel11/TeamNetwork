"use client";

/**
 * Command palette primitives (cmdk). Use {@link Command} from "cmdk" for nested
 * Command.Input / Command.List / Command.Item, or import this re-export.
 */
export { Command } from "cmdk";
